---
import NavLink from './NavLink.astro';
import { SITE_TITLE } from '../consts';
import { NAV_LINKS } from '../consts';
---

<header class="bg-white border-b border-gray-200 sticky top-0 left-0 right-0 z-50">
	<div class="container mx-auto px-2 py-4">
		<nav class="flex justify-between items-center gap-4">
			<h2 class="text-2xl font-black"><a href="/" class="!text-black">{SITE_TITLE.slice(0,3)}<span class="font-light">ify</span> </a></h2>
			<div class="flex justify-center gap-4 hidden md:flex">
				{
					NAV_LINKS.map((item) => (
						<NavLink href={item.href}>{item.label}</NavLink>
					))
				}
			</div>
	</div>
	</nav>
</header>